import 'dart:html';

import 'package:flutter/material.dart';

abstract class Screen2 extends StatelessWidget {
  const Screen2({super.key, required this.name});
  final
  @override
  Widget build(BuildContext context) {
    return const Scaffold(

    );
  }
}
